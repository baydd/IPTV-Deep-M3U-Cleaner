# IPTV-Deep-M3U-Cleaner
 A desktop tool that **deeply validates IPTV streams** inside `.m3u` / `.m3u8` playlists using **FFprobe**.  Unlike simple HTTP checks, this tool verifies whether a stream actually contains a **real video stream**.
